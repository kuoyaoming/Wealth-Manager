<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Exclude sensitive data from backup -->
    <exclude domain="database" path="wealth_manager_database"/>
    <!-- Exclude encrypted user API keys -->
    <exclude domain="sharedpref" path="api_keys_prefs.xml"/>
</full-backup-content>