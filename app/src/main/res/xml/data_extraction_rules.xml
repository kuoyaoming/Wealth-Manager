<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <cloud-backup>
        <!-- Exclude sensitive financial data from cloud backup -->
        <exclude domain="database" path="wealth_manager_database"/>
    </cloud-backup>
    <device-transfer>
        <!-- Exclude sensitive data from device transfer -->
        <exclude domain="database" path="wealth_manager_database"/>
    </device-transfer>
</data-extraction-rules>